{
  "name": "coletor-de-dados",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check:netlify": "node scripts/check-netlify-config.mjs",
    "build:chat": "cd frontend && npm install && npm run build",
    "postbuild:chat": "node scripts/sync-chat-build.mjs",
    "build": "npm run check:netlify && npm run build:chat && npm run postbuild:chat"

  },
  "engines": {
    "node": ">=22"

  }
}
