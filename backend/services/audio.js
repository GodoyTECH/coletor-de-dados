/**
 * Placeholder para transcrição futura.
 * Aqui você pode plugar Whisper local, OpenAI Whisper API ou outro provider.
 */
export async function transcribeAudioPlaceholder(filePath) {
  return {
    text: '[TRANSCRIÇÃO_PENDENTE] áudio recebido com sucesso',
    provider: 'placeholder',
    filePath
  };
}
