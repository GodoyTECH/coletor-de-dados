# coletor-de-dados
teste 1

## Configuração IA (Gemini)
- Defina a variável de ambiente `GEMINI_API_KEY` nas configurações do site (Netlify) para habilitar a estruturação via IA.
- O recurso é controlado por feature flag no frontend (`ENABLE_AI_STRUCTURING` em `ocr.js`).
